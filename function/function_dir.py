import sys
# dir函数能够返回由对象所定义的名称列表,如果这一对象是一个模块，则该列表会包括函数内所定义的函数、类与变量。
# 该函数接受参数。 如果参数是模块名称，函数将返回这一指定模块的名称列表。 如果没有提供参数，函数将返回当前模块的名称列表
print(dir(sys))
print(dir())
a = 5
print(dir())
del a
print(dir())
print(dir(str))# 可以访问 str（String，字符串）类的属性。
# vars() 函数也可以返回给你这些值的属性，但只是可能，它并不能针对所有类都能正常工作。

